{% extends 'base.html.twig' %}

{% block title %}Edición de herramienta{% endblock %}

{% block body %}

    <div class="container h-100">
        <div class="row h-100 justify-content-center align-items-center">
            <div class="col-9 bg-white rounded my3 p-5">
                <h1>Edición de herramienta</h1>
                <a href="{{ path('tool_index') }}"><i class="fas fa-hand-point-left"> Volver a la lista</i></a>
                {{ include('tool/_form.html.twig', {'button_label': 'Update'}) }}
                {% if is_granted("ROLE_MANAGER") %}
                    {{ include('tool/_delete_form.html.twig') }}
                {% endif %}
            </div>
        </div>
    </div>

{% endblock %}
