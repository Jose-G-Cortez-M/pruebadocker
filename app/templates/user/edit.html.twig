{% extends 'base.html.twig' %}

{% block title %}Editar empleado{% endblock %}

{% block body %}
    <div class="container h-100">
        <div class="row h-100 justify-content-center align-items-center">
            <div class="col-9 bg-white rounded my3 p-5">
                <h1>Editar empleado</h1>
                <h4> Por seguridad, cada vez que modifica a un empleado, se debe cambiar la contraseña </h4>
                {{ include('user/_form.html.twig', {'button_label': 'Update'}) }}
                {{ include('user/_delete_form.html.twig') }}
                <a href="{{ path('user_index') }}"><i class="fas fa-hand-point-left"> Volver a la lista</i></a>
            </div>
        </div>
    </div>
{% endblock %}
